# Prototype V0.1 开发任务列表

## 执行规则

- 每个任务必须能追溯到四份交付文档。
- 完成一个任务后更新本文档状态。
- 不允许用假数据、默认值或静默 fallback 掩盖业务缺口。
- 进入下一任务前必须提交当前任务成果。

## 任务状态

- [ ] Task 1：Godot 工程骨架
  - 状态：已完成基线骨架，后续按需求扩展。
  - 验证：Godot 4.6.2 headless 启动主场景成功。

- [x] Task 2：数据导入和校验层
  - 状态：已完成。
  - 范围：测试数据、主键、必填字段、枚举、外键校验。
  - 验收：缺字段、重复 ID、非法枚举、非法外键必须失败。
  - 验证：
    - `test_data_validator.gd` 通过。
    - `test_core_data_loader.gd` 通过。

- [x] Task 3：核心状态模型
  - 状态：已完成。
  - 范围：从已校验数据集生成运行时状态索引。
  - 验收：城市、势力、武将、路线按 ID 可读取；初始部队为空；无效数据不会创建运行时状态。
  - 验证：
    - `test_core_state_factory.gd` 通过。

- [x] Task 4：时间推进
  - 状态：已完成。
  - 范围：日推进、30 天月度 tick、非法推进天数失败。
  - 验收：推进天数必须改变真实运行时状态；0 或负数推进必须报错。
  - 验证：
    - `test_time_system.gd` 通过。

- [x] Task 5：任命和出阵
  - 状态：已完成。
  - 范围：同势力太守任命、出阵校验、城市扣兵扣粮、生成部队。
  - 验收：敌方武将不能任命本城；兵粮不足不能出阵；失败时不得污染城市状态。
  - 验证：
    - `test_appointment_and_sortie.gd` 通过。

- [x] Task 6：行军和粮草
  - 状态：已完成。
  - 范围：启动行军、计算预计天数、每日推进、粮草消耗、缺粮状态。
  - 验收：距离 60、速度 12 时预计 5 天抵达；行军不能瞬移；粮草不足进入 `out_of_supply`。
  - 验证：
    - `test_march_and_supply.gd` 通过。

- [x] Task 7：自动战斗和占领回流
  - 状态：已完成。
  - 范围：接敌部队自动结算、胜负记录、损兵、城市归属变化、占领状态回流。
  - 验收：未接敌部队不能战斗；目标城必须是路线终点；胜利后城市归属和部队战斗结果必须写回运行时状态。
  - 验证：
    - `test_battle_and_occupation.gd` 通过。

- [x] Task 8：战后整合
  - 状态：已完成。
  - 范围：占领后新占惩罚、整合进度、民心治安恢复、恢复完成回到 normal。
  - 验收：未占领城市不能执行战后整合；占领城市不能立即满收益；整合必须显式推进。
  - 验证：
    - `test_postwar_integration.gd` 通过。

- [x] Task 9：调试 UI
  - 状态：已完成。
  - 范围：调试 UI 状态快照、基础调试面板脚本和场景。
  - 验收：显示数据必须来自真实运行时状态；缺少关键状态时必须报错，不允许空白假展示。
  - 验证：
    - `test_debug_state_presenter.gd` 通过。

- [ ] Task 10：存档读档
  - 状态：下一任务。
