# 数据导入层

职责：

- 读取测试数据。
- 校验主键、必填字段、枚举和外键。
- 把静态主数据交给模拟层使用。
- 读取和校验 Content Alpha 阶段性内容索引，例如英雄半身像候选资源索引。
- 读取和校验 Content Alpha 候选资源包清单，避免来源项目、所有权状态、来源路径和允许用途只停留在文档说明。
- 后续工具和 UI 应通过统一 loader 读取 Content Alpha 资源清单，不直接绕过校验解析 JSON。
- 资源专用 loader 应从资源清单解析具体资源包，再加载对应索引，避免 UI 层硬编码数据文件路径。
- 阶段性内容验收应通过聚合校验入口串联清单、资源包、索引、预览行和真实资源解码，避免只验证单个文件存在。
- 英雄半身像导入清单必须校验项目内 PNG 的路径、大小、哈希、尺寸和英雄绑定，避免导入目录与审计索引脱节。
- 可复用半身像池只允许记录图片资源、尺寸、哈希和源英雄名参考，禁止泄漏源项目技能、传记、君略、数值、官职或势力等玩法字段。
- 候选武将名册只允许记录身份、选择状态和头像绑定，禁止写入正式属性、技能、势力、官职、传记或君略内容。
- 调试面板默认预览应读取可复用半身像池，不再把源项目 426 条英雄绑定当作本项目候选武将列表。
- UI 信息架构规格必须显式区分 `debug_available / content_alpha_available / planned`；`planned` 界面必须声明阻塞项，且规格必须说明它不是 Beta 正式 UI，避免把规划页面误当作已实现玩法入口。
- UI 线框规格必须显式声明布局区域、组件、状态绑定、交互和阻塞项；它只能作为正式 UI 实装合同，不允许把缺少正式数据源的页面包装成可玩界面。
- UI 主题 Token 必须通过统一 loader 校验色板、字号、间距、圆角、控件状态和响应式规则；控件状态引用的颜色必须来自同一色板，不允许临时写散色值。

禁止：

- 用默认值掩盖关键字段缺失。
- 静默跳过非法数据。
- 自动修正异常业务状态。
